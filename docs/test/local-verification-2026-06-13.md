# 本地验证记录：2026-06-13

## 1. 验证目标

确认《多源异构大数据处理与质量治理平台》在本轮改版后具备稳定启动入口、可运行前端、可操作业务表单、页面内验收闸口、交付材料入口和剩余风险展示，并完成桌面端与 390px 移动端视觉验收。

## 2. 执行环境

- 工作目录：`F:\软件开发\big-data-processing-platform`
- Node 要求：`>=18`
- 启动入口：`npm.cmd start`
- 构建入口：`npm.cmd run build`
- 预览入口：`npm.cmd run preview`

## 3. 自动化验证

执行命令：

```powershell
npm.cmd run build
npm.cmd test
npm.cmd run test:visual
```

验证结果：

```text
Static frontend build passed: dist/
API smoke tests passed
Visual QA passed: http://127.0.0.1:<dynamic-port>
```

覆盖点：

- 静态构建产物可生成
- 健康检查、项目定位、角色、流程、页面、工作流、质量、资产、审计、验收接口可访问
- 新增验收中心接口 `/api/acceptance/center` 返回准备度、发布闸口、交付材料和剩余风险
- 桌面端与 390px 移动端无横向溢出、无控制台错误
- 首屏存在“运行控制中心”“今日重点”“验收闸口”
- 操作中心存在“发布闸口”
- 验收中心存在“交付材料”“剩余风险”

## 4. 本轮界面与交互验收

已完成：

- 强化 Swiss 工程控制台层级，保持单蓝色强调、细分隔线和左对齐扫描路径
- 在首屏右侧新增“验收闸口”，直接暴露构建、接口、视觉和材料状态
- 在操作中心新增“发布闸口”，把本轮收口项从文档外移到页面内
- 在验收中心新增“交付材料”“剩余风险”，形成页面内闭环
- 保留并验证三个核心可操作表单：
  - `sourceRequestForm`
  - `taskTransitionForm`
  - `reportCenterForm`

## 5. 访问地址

- 前端工作台：`http://127.0.0.1:5214`
- API：`http://127.0.0.1:8214`
- 预览：`http://127.0.0.1:6214`
- 前端代理健康检查：`http://127.0.0.1:5214/api/ops/health`
- API 健康检查：`http://127.0.0.1:8214/api/ops/health`

## 6. 当前结论

当前项目已具备“独立产品前端 + Node API + 页面内验收闸口 + 文档可追溯 + 自动化验证”的本地验收条件。

剩余风险：

- 表单状态仍为前端会话态，刷新后不持久化
- 任务调度与工作流状态仍为演示数据，未接入真实执行器
- 权限矩阵为展示态，尚未接真实认证授权链路
